{
  "name": "amen24",
  "version": "1.0.0",
  "description": "Amen24 is a free non-profitable project to introduce bible content for all.",
  "main": "ecosystem.config.js",
  "scripts": {
    "build": "npm --prefix packages/shared run build && npm --prefix packages/ui run build",
    "dev": "concurrently -n backend,frontend,admin -c blue,green,yellow \"npm --prefix apps/backend run start:dev\" \"npm --prefix apps/frontend run dev\" \"npm --prefix apps/admin run dev\""
  },
  "author": "emadunan",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
